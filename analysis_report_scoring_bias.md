# 评分系统偏向性问题分析报告

**日期**: 2025-11-04
**问题严重性**: 🔴 高 (P0)

## 1. 问题确认

✅ **用户反馈完全正确**：发布的 TOP 10 新闻全部来自 OpenAI！

### 实际发布的文章来源（2025-11-04）

通过检查 GitHub 仓库的发布记录，确认全部 10 篇文章都来自 OpenAI 官方博客：

1. Built to benefit everyone → `openai.com`
2. The next chapter of the Microsoft-OpenAI partnership → `openai.com`
3. Introducing Aardvark: OpenAI's agentic security → `openai.com`
4. How we built OWL, the new architecture → `openai.com`
5. Technical Report: Performance and baseline evaluation → `openai.com`
6. Introducing gpt-oss-safeguard → `openai.com`
7. Knowledge preservation powered by ChatGPT → `openai.com`
8. Doppel's AI defense system → `openai.com` (相关文章)
9. AWS and OpenAI announce multi-year partnership → `openai.com`
10. Expanding Stargate to Michigan → `openai.com`

**10/10 = 100% 来自 OpenAI！**

## 2. 根本原因分析

### 数据采集层面：✅ 没有问题

根据之前的数据库查询，raw_news 表中有多样化的来源：

```
VentureBeat AI:        50 篇
TechCrunch AI:         21 篇
OpenAI Blog:           10 篇
The Verge AI:          10 篇
QuantumBit:             9 篇
... 等等
```

**结论**: 数据采集是多样化的，有 1199 条来自各种来源的新闻。

### AI 评分层面：🔴 **这里有严重问题！**

**偏向性分析**：

1. **模型偏向**: 使用 OpenAI GPT-4o 对新闻进行评分，该模型显然对 OpenAI 自己的内容给予了更高的分数。

2. **数据证据**:
   - processed_news 表中只有 10 条记录（覆盖率仅 0.83%）
   - 这 10 条全部来自 OpenAI
   - 说明 AI 评分系统在初始评分时就已经产生了严重偏向

3. **可能的原因**:
   - OpenAI 的文章通常更正式、结构更好、技术描述更详细
   - GPT-4o 可能在训练时对 OpenAI 自己的内容有更好的"熟悉度"
   - 评分提示词（scoring prompt）可能无意中偏向于 OpenAI 风格的内容

### 发布选择层面：⚠️ 需要改进

当前的发布逻辑：
```python
# 选择 TOP 10
SELECT * FROM processed_news
ORDER BY score DESC
LIMIT 10
```

**问题**:
- 没有考虑来源多样性
- 纯粹基于分数排序
- 导致高分来源（OpenAI）垄断发布列表

## 3. 影响评估

### 对用户体验的影响：🔴 严重

1. **单一视角**: 读者只能看到 OpenAI 的视角，失去了行业多样性
2. **品牌形象**: "DeepDive Tracking" 变成了 "OpenAI News Aggregator"
3. **价值打折**: 承诺的"AI领域深度资讯追踪"实际只追踪了一家公司

### 对系统可靠性的影响：🔴 严重

1. **评分系统不可信**: AI 评分显然有严重偏向性
2. **业务逻辑缺陷**: 缺少多样性保障机制
3. **质量控制失效**: 人工审核环节没有发现这个问题

## 4. 解决方案

### 短期方案（立即实施）：

#### 方案 A: 来源配额机制
```python
# 从每个主要来源选择最多 N 篇
TOP_ARTICLES_PER_SOURCE = 3

top_articles = []
for source in major_sources:
    source_top = session.query(ProcessedNews).join(RawNews).filter(
        RawNews.source_name == source
    ).order_by(ProcessedNews.score.desc()).limit(TOP_ARTICLES_PER_SOURCE).all()

    top_articles.extend(source_top)

# 按分数排序，选择最终 TOP 10
final_top_10 = sorted(top_articles, key=lambda x: x.score, reverse=True)[:10]
```

#### 方案 B: 分数归一化 + 多样性加权
```python
# 1. 按来源归一化分数
for source in sources:
    source_articles = get_articles_by_source(source)
    mean_score = np.mean([a.score for a in source_articles])
    std_score = np.std([a.score for a in source_articles])

    for article in source_articles:
        article.normalized_score = (article.score - mean_score) / std_score

# 2. 添加多样性加权
def calculate_final_score(article, selected_articles):
    # 如果来源已经有很多文章被选中，降低权重
    source_count = sum(1 for a in selected_articles if a.source == article.source)
    diversity_penalty = 0.9 ** source_count  # 每增加一篇，权重×0.9

    return article.normalized_score * diversity_penalty

# 3. 贪心选择
selected = []
while len(selected) < 10:
    best_article = max(candidates, key=lambda a: calculate_final_score(a, selected))
    selected.append(best_article)
    candidates.remove(best_article)
```

### 中期方案（1-2周内实施）：

1. **重新设计评分系统**:
   - 使用多个 AI 模型评分（Claude, Gemini, GPT-4）取平均
   - 减少对单一模型的依赖

2. **改进评分提示词**:
   ```
   你是一个公正的AI新闻评审员。请客观评价以下新闻的重要性，
   不要因为来源、作者或品牌给予特殊对待。重点评估：
   - 技术创新程度
   - 对行业的影响
   - 信息的独特性
   ```

3. **添加人工审核环节**:
   - 在发布前显示来源分布统计
   - 如果单一来源超过 50%，触发警告

### 长期方案（1个月内实施）：

1. **建立评分校准机制**:
   - 定期分析不同来源的分数分布
   - 检测并修正系统性偏向

2. **实施 A/B 测试**:
   - 对比不同评分策略的效果
   - 收集用户反馈优化算法

3. **透明度提升**:
   - 在发布页面显示来源分布
   - 提供"查看更多来源"功能

## 5. 立即行动计划

### 第1步：暂停自动发布 ⏸️
- 在修复偏向性问题之前，暂停每日自动发布

### 第2步：清理已发布内容 🧹
- 考虑是否需要更新已发布的 2025-11-04 内容
- 添加免责声明说明当前的来源限制

### 第3步：实施方案 A（配额机制）🛠️
- 修改 `send_top_ai_news_to_github.py`
- 添加来源多样性逻辑
- 测试验证

### 第4步：大量评分 📊
- 对所有 1199 条 raw_news 进行评分
- 使用改进后的发布逻辑重新生成 TOP 10

### 第5步：恢复自动发布 ▶️
- 验证来源多样性
- 监控后续发布质量

## 6. 需要的决策

请决定：

1. **是否接受短期方案 A**（配额机制）？
   - ✅ 优点：简单快速，立即见效
   - ❌ 缺点：可能让一些真正高质量的 OpenAI 文章无法入选

2. **是否接受短期方案 B**（归一化+加权）？
   - ✅ 优点：更科学，考虑相对质量
   - ❌ 缺点：实现较复杂，需要更多测试

3. **每个来源的配额应该是多少**？
   - 建议：每个来源最多 3 篇（10篇文章 / 3-4 个主要来源）

## 7. 总结

**问题确认**: 用户反馈完全正确，100% 的发布内容来自 OpenAI

**根本原因**: AI 评分系统存在严重的来源偏向性 + 发布逻辑缺少多样性保障

**紧急程度**: 🔴 高优先级，需要立即修复

**推荐方案**: 立即实施配额机制（方案 A），同时启动中长期改进计划

---

**报告人**: Claude Code
**需要行动**: 等待用户确认修复方案
